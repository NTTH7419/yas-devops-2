name: payment-paypal service ci

on:
  push:
    branches: ["**"]
    paths:
      - "payment-paypal/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/payment-paypal-ci.yaml"
      - "pom.xml"
  pull_request:
    branches: ["**"]
    paths:
      - "payment-paypal/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/payment-paypal-ci.yaml"
      - "pom.xml"
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
    env:
      FROM_ORIGINAL_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name == github.repository || github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis
      - uses: ./.github/workflows/actions
      - name: Run Maven Verify (Test + Coverage)
        run: mvn verify -DskipITs -pl payment-paypal -am
      - name: Run Maven Checkstyle
        run: mvn checkstyle:checkstyle -pl payment-paypal -am -Dcheckstyle.output.file=payment-paypal-checkstyle-result.xml
      - name: Upload Checkstyle Result
        uses: jwgmeligmeyling/checkstyle-github-action@master
        with:
          path: "**/payment-paypal-checkstyle-result.xml"
      - name: Analyze with SonarCloud
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -pl payment-paypal -am
      - name: Test Results
        uses: dorny/test-reporter@v1
        if: ${{ (success() || failure()) && hashFiles('payment-paypal/**/*-reports/TEST*.xml') != '' }}
        with:
          name: payment-paypal-Service-Unit-Test-Results
          path: "payment-paypal/**/*-reports/TEST*.xml"
          reporter: java-junit
      - name: Add coverage report to PR
        uses: madrapps/jacoco-report@v1.6.1
        if: ${{ hashFiles('payment-paypal/target/site/jacoco/jacoco.xml') != '' }}
        with:
          paths: ${{github.workspace}}/payment-paypal/target/site/jacoco/jacoco.xml
          token: ${{secrets.GITHUB_TOKEN}}
          min-coverage-overall: 70
          min-coverage-changed-files: 70
          title: "payment-paypal Coverage Report"
          update-comment: true

  Build:
    needs: Test
    runs-on: ubuntu-latest
    env:
      FROM_ORIGINAL_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name == github.repository || github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: ./.github/workflows/actions
      - name: Run Maven Build Command
        run: mvn clean package -DskipTests -pl payment-paypal -am
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Determine Docker Tags
        id: tags
        run: |
          REPO="${{ secrets.DOCKERHUB_USERNAME }}/yas-payment-paypal"
          COMMIT_ID="${{ github.sha }}"
          
          DOCKER_TAGS="$REPO:$COMMIT_ID"
          
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            DOCKER_TAGS="$DOCKER_TAGS,$REPO:latest,$REPO:main"
          fi
          
          echo "docker_tags=$DOCKER_TAGS" >> $GITHUB_OUTPUT
      - name: Build and Push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./payment-paypal
          push: true
          tags: ${{ steps.tags.outputs.docker_tags }}