name: backoffice service ci

on:
  push:
    branches: ["**"]
    paths:
      - "backoffice/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/backoffice-ci.yaml"
      - ".snyk"
  pull_request:
    branches: ["**"]
    paths:
      - "backoffice/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/backoffice-ci.yaml"
      - ".snyk"
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    env:
      FROM_ORIGINAL_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name == github.repository || github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci --ignore-scripts
        working-directory: backoffice
      - run: npm run lint
        working-directory: backoffice
      - run: npx prettier --check .
        working-directory: backoffice
      - name: Snyk - Dependency Scan
        run: >
          npx snyk test 
          --file=backoffice/package.json
          --org=${{ vars.SNYK_ORG }}
          --severity-threshold=high
          --policy-path=.snyk
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true
      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v5
        with:
          projectBaseDir: backoffice
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  Build:
    needs: Test
    runs-on: ubuntu-latest
    env:
      FROM_ORIGINAL_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name == github.repository || github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci --ignore-scripts
        working-directory: backoffice
      - run: npm run build
        working-directory: backoffice
      # - name: Log in to the Container registry
      #   if: ${{ github.ref == 'refs/heads/main' }}
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      # - name: Build Docker image
      #   if: ${{ github.ref == 'refs/heads/main' }}
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./backoffice
      #     push: false
      #     load: true
      #     tags: ghcr.io/nashtech-garage/yas-backoffice:latest
      # - name: Install Snyk CLI
      #   run: npm install -g snyk
      # - name: Snyk - Container Scan
      #   if: ${{ github.ref == 'refs/heads/main' }}
      #   continue-on-error: true
      #   env:
      #     SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      #   run: >
      #     snyk container test ghcr.io/nashtech-garage/yas-backoffice:latest 
      #     --file=backoffice/Dockerfile 
      #     --org=ntth7419 
      #     --project-name="yas/backoffice-container" 
      #     --severity-threshold=high
      # - name: Push Docker image
      #   if: ${{ github.ref == 'refs/heads/main' }}
      #   run: docker push ghcr.io/nashtech-garage/yas-backoffice:latest
