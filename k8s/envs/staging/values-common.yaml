backend:
  image:
    tag: latest
    pullPolicy: Always
  ingress:
    enabled: true
    host: "api.staging.yas.local.com"
  service:
    type: NodePort
    nodePort: null

ui:
  service:
    type: NodePort
    nodePort: null

mediaApplicationConfig:
  yas:
    publicUrl: http://api.staging.yas.local.com/media

